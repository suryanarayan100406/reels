{
  "name": "if-you-ever-wondered",
  "version": "1.0.0",
  "private": true,
  "description": "A hand-curated Instagram reel gallery — a personal gift site",
  "scripts": {
    "dev:server": "cd server && node src/index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build:render": "npm run install:all && npm run build",
    "start": "cd server && node src/index.js"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3",
    "bcrypt": "^6.0.0",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "helmet": "^8.1.0",
    "pg": "^8.20.0"
  }
}
